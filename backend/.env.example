PORT=5000
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/portfolio?retryWrites=true&w=majority
FRONTEND_URL=http://localhost:5173

# Optional: email (Nodemailer). If not set, messages are only stored in DB.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=portfolio@yourdomain.com
# CONTACT_EMAIL=your@email.com
